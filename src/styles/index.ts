import { COLORS } from './colors/colors';
import FONTS from './fonts/fonts';
import {
  DURATIONS,
  HEIGHTS,
  SCREEN_WIDTHS,
  Z_HEIGHTS,
} from './constants/constants';

export { COLORS, DURATIONS, FONTS, HEIGHTS, SCREEN_WIDTHS, Z_HEIGHTS };
